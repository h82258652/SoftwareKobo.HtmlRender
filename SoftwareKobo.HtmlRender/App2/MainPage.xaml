<Page x:Class="App2.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:App2"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ff="SoftwareKobo.HtmlRender.Core"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:core="using:SoftwareKobo.HtmlRender.Core"
      mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Click="ButtonBase_OnClick" Content="1111111111111"></Button>
        <ScrollViewer Grid.Row="1">
            <!--
        <RichTextBlock Grid.Row="1" core:HtmlRenderHelper.Html="{Binding Path=HH}" IsTextSelectionEnabled="False" FontSize="16"></RichTextBlock>
            -->
            <WebView x:Name="ff"></WebView>
        </ScrollViewer>
    </Grid>
</Page>